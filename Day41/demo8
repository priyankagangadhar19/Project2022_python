#in python can we write a function inside another function? Yes -inner/nested function
#from where we can call the inner function - inside the outer function where inner function is present
#parameter of outter function can be directly used by inner function

def outer(msg):
    print('this is outer function',msg)

    def inner():
        print('this is inner function:',msg)

    inner()

outer('Bhanu')



#in python can we write a function inside another function? Yes -inner/nested function
#from where we can call the inner function - inside the outer function where inner function is present
#parameter of outter function can be directly used by inner function


def outer(msg):
    def inner():
        print('Inner function start')
        print(msg)
        print('Inner function end')

    inner()

outer('Bhanu')
